[project]
name = "car-detection"
version = "0.1.0"
description = "Arduino-based dual traffic light controller with serial communication"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyserial>=3.5",
    "opencv-python>=4.8.0",
    "ultralytics>=8.0.0",
]
